apiVersion: v1
kind: PersistentVolume
metadata:
  name: syncthing-pv-config
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-storage
  nfs:
    server: 192.168.0.20
    path: /volume2/cluster-storage/syncthing/config
  mountOptions:
    - nfsvers=4.1
